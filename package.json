{
  "name": "webhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create": "claudia create --region us-east-1 --profile default  --role lambda_basic_execution --api-module app --name messageBOT",
    "update": "claudia update"


  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "claudia": "^2.14.2",
    "claudia-api-builder": "^2.5.1",
    "request": "^2.81.0"
  }
}
